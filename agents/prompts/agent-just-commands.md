---
description: Process the just commands into agent prompts
---

Process the just commands into agent prompts
Category: misc.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just agent-just-commands
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
