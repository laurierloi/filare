---
description: Validate backlog headers and UID formats
---

Validate backlog headers and UID formats
Category: project_management.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just check-backlog-headers
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
