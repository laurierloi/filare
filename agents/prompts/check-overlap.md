---
description: Check for overlapping elements in all generated HTML files in outputs/
---

Check for overlapping elements in all generated HTML files in outputs/
Category: filare.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just check-overlap
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
