---
description: '---- Tooling ----  Check that all required CLI tools are installed (uses
  your check-tools.sh)'
---

---- Tooling ----  Check that all required CLI tools are installed (uses your check-tools.sh)
Category: lint.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just check-tools
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
