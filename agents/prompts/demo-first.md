---
description: Build only the first example with filare (quick sanity run)
---

Build only the first example with filare (quick sanity run)
Category: filare.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just demo-first
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
