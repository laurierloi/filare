---
description: '---- Filare commands & docs ----  Build only the first example with
  filare (quick sanity run)'
---

---- Filare commands & docs ----  Build only the first example with filare (quick sanity run)
Category: filare.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just example-first
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
