---
description: Show resolved Filare settings (YAML, includes defaults)
---

Show resolved Filare settings (YAML, includes defaults)
Category: filare.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just filare-settings-get
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
