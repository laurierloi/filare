---
description: Orchestrator instructs a FIXER agent to read structured review data and
  apply feedback to the codebase, then run lint-and-test-fast.
---

Orchestrator instructs a FIXER agent to read structured review data and apply feedback to the codebase, then run lint-and-test-fast.
Category: project_management.
Marked unsafe for automatic agents — confirm before running.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && true
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
