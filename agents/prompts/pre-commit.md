---
description: Run pre-commit only on staged/changed files
---

Run pre-commit only on staged/changed files
Category: lint.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just pre-commit
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
