---
description: Export backlog to Taskwarrior JSON (filters can be passed as CLI args)
---

Export backlog to Taskwarrior JSON (filters can be passed as CLI args)
Category: project_management.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just taskwarrior-export
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
