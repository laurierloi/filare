---
description: Only functional tests (expects tests marked with "functional")
---

Only functional tests (expects tests marked with "functional")
Category: test.

Command to run inside the repo:
```bash
source scripts/agent-setup.sh >/dev/null && just test-functional
```
After execution, summarize the outcome succinctly and mention any errors or next steps.
